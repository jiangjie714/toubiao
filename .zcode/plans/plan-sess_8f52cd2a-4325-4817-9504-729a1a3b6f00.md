# 计划：完成《技术架构方案》剩余章节写入

## 现状

`docs/技术架构方案.md` 第一部分（设计目标/总体架构/数据模型设计，第一至三章）已成功写入。因系统处于 plan mode 只读限制，第四至十二章的追加写入被拦截。

## 待执行内容（内容已定稿，仅剩文件写入）

向该文档追加以下章节（沿用已提交并获批的方案内容）：

- **第四章 采集技能体系 v2**：zod 配置 Schema（FieldSpec 扩展 const、extraFields、detail.extraction）、白名单 EXTRA_FIELD_TARGETS（9 个语义字段 + 类型转换器）、内建兜底抽取正则组（置信度 0.6）、RunContext 统一 dry-run/live 路径、配置校验器、版本化「只前进」流程
- **第五章 采集配置中心**：DB 准绳/文件开发态的 loadCompiledConfig 热更新流程、7 个管理页面清单（源列表/新建/编辑/dry-run/历史/告警）、权限与 ReDoS 防护
- **第六章 调度器与任务队列**：scheduler 每分钟 due 检查（nextRunAt 时间戳法 + cron-parser）、CrawlTask 表抢占式消费、失败指数退避、全局并发上限、同域名错峰
- **第七章 健康分与监控告警**：score = 100 − 连续失败×15 − 7日失败率×40 − 新鲜度超期 20，三档分级与告警触发
- **第八章 数据丰富化管道**：7 阶段管道（快照→normalize→分节→config 抽取→内建兜底→转换校验→字段级 diff 入库）、人工字段保护、多源仲裁、项目聚合算法（编号精确 + 标题 bigram 相似度 ≥0.82）、附件下载与快照生命周期
- **第九章 PostgreSQL 迁移**：五步迁移 + LIKE 大小写差异（PG 需 insensitive）+ 回滚策略
- **第十章 来源接入路线**：P0 两省试点 → P0 全量 → P1 央企/垂直，每源接入 SOP（分析→config→dry-run→48h 观察→启用）
- **第十一章 代码结构与里程碑映射**：crawler/ 新增模块清单与 M0 周次对应
- **第十二章 测试与验收**：单测/集成/数据验收清单

## 收尾

- 与《商业化计划书》交叉校对引用
- AGENTS.md / README 加一行指引

批准后即可一次性完成写入（约 4 次文件编辑，纯文档，不改任何代码）。